
#include <stdint.h>

int main(void)
{

	uint32_t *Clk 		= (uint32_t*) 0x40023830;
	uint32_t *GpioDER 	= (uint32_t*) 0x40020C00;
	uint32_t *GpioDOut 	= (uint32_t*) 0x40020C14;

	*Clk 		|= (1 << 3);
	*GpioDER 	&= ~(1 << 24);
	*GpioDER 	|= (1 << 24);

	while(1){
		for (uint32_t i=0; i<=100000; i++) *GpioDOut = (1 << 12);

		for (uint32_t i=0; i<=100000; i++) *GpioDOut &= ~(1 << 12);

	}
}
